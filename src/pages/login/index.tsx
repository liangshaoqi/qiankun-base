import React, { useState, useEffect } from 'react';

interface Props {
  
}

const Login: React.FC<Props> = (props) => {

  const {} = props

  const [] = useState()

  useEffect(() => {}, [])

  return (
    <div>
      登录页面
    </div>
  );
};

export default Login;